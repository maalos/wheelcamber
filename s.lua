addEvent("responseDoesTheVehicleHaveACustomWheelSet", true)  -- THIS ONE IS UNTESTED, USE AS A REFERENCE ONLY
addEventHandler("responseDoesTheVehicleHaveACustomWheelSet", root, function(response)
    if response == true then
        -- callback() -- reference only
    end
end)